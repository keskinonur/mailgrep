# Azure AD App Registration (required)
# Create at: https://portal.azure.com → App registrations
# See README.md for setup instructions
AZURE_CLIENT_ID=
AZURE_TENANT_ID=

# Optional defaults (leave empty for interactive prompts)
DEFAULT_SENDER=
DEFAULT_OUTPUT_DIR=

# File types to download (comma-separated extensions, without dots)
# Default: png,jpg,jpeg,gif,bmp,webp,tiff (images only)
# Examples:
#   FILE_TYPES=png,jpg,pdf                    # Images + PDFs
#   FILE_TYPES=pdf,docx,xlsx,zip              # Documents only
#   FILE_TYPES=*                              # All attachments
FILE_TYPES=
